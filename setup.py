#!/usr/bin/env python3
# vim: ts=4 sw=4 et
# -*- coding: utf-8 -*-

# This project has all configuration in setup.cfg.
# This file remains only for anything that expects to see it.

# You should not intentionally execute this file.
# Instead, you can use a command like this:
#   pip install /path/to/this/directory
# Assuming your package maanger is new enough, it will know what to do.

import setuptools
setuptools.setup()
